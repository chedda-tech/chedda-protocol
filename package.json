{
  "name": "chedda-protocol",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "coverage": "forge coverage",
    "coverage-report": "forge coverage --report lcov && lcov --remove ./lcov.info -o ./lcov.info.pruned 'script' 'test' && genhtml lcov.info.pruned --branch-coverage --output-dir coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.14.1",
    "hardhat-preprocessor": "^0.1.5"
  },
  "dependencies": {
    "solidity-docgen": "^0.6.0-beta.36"
  }
}
